import React from 'react';
import { ProductUpdationForm } from './ProductUpdationForm';

export const UpdateExistingProductComponent = (props) => {
    return (
        <ProductUpdationForm />
    );
}